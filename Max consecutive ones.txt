#include <stdio.h>
int main()
{
    int a[100],o[50],n,c=1,i,j,k=0,m=0;
    printf("Enter number of elements :");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for (i=0;i<n-1;i++)
    {
        if (a[i]==a[i+1] && a[i]==1)
        {
            c++;
        }
        else
        {
            if (m<c)
            {
                m=c;
            }
            c=1;
        }
        if (m<c)
            {
                m=c;
            }
        k++;
    }
    printf("\nmax consecutive ones are ");
    if (m==1)
        printf("%d",0);
    else 
        printf("%d",m);
}